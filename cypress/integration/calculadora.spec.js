/// <reference types="cypress" />

describe('deve garantir que as operações estão funcionando corretamente', () => {
  it('', () => {
    cy.visit('http://localhost:4200/')
    expect()
  });

});
// testar se aparece a tela da calculadora
// testar as operaços basicas + - / *
// testar se o botao on zera
// testar a quantidade de caracteres permitidos
// testar numeros decimais e negativos
